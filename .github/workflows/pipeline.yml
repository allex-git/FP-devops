name: pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: set up docker buildx
        uses: docker/setup-buildx-action@v3

      - name: login to docker hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: generate commit sha
        id: vars
        run: echo "TAG=$(echo $GITHUB_SHA | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: build and push docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/final-project:${{ steps.vars.outputs.TAG }}

      # - name: update kubernetes deployment
      #   run: |
      #     sed -i "s|image: .*|image: ${{ secrets.DOCKERHUB_USERNAME }}/final-project:${{ steps.vars.outputs.TAG }}|g" kubernetes/deployment.yaml

      # - name: commit and push changes
      #   run: |
      #     git config user.name "github-actions"
      #     git config user.email "github-actions@github.com"
      #     git add kubernetes/deployment.yaml
      #     git commit -m "pipline: update image tag to ${{ steps.vars.outputs.TAG }}" || echo "no change"
      #     git push